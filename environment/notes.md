# Environment notes

The `multiprocessing` library is part of Python's standard library. You do not need to install it separately.

These examples intentionally avoid NumPy, PyTorch, TensorFlow, mpi4py, and other external packages so environment setup stays simple.
